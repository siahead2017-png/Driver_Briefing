# Бриф (18.08.2026)

Владелец провёл ещё один живой инструктаж под аудиозапись и приложил транскрибацию
(файл `WhatsApp_Audio_2026-08-18_compressed.txt`, расшифрован вручную из
повреждённой кодировки).

Дословный запрос:

> Смотри - я провел еще одну инструкцию под запись. Транскрибацию приложил.
>
> Посмотри - какие у меня были ошибки в инструктаже.
> Сравни его с нашей версией и давай наш электронный брифинг пересмотрим -
> сделаем его структурнее, что-то какието пунты объединим или уберем.
>
> Можешь подключить /autopilot interview и пройти по вопросам меня прожарить

## Контекст, который уже известен (не переспрашивать)

- Проект: `Driver_Briefing` — сценарий для ведущего инструктажа (не справочник
  водителю). Модель данных — `data/briefing.json`, блоки `guide` / `cheatsheet`
  / `contacts` / `blocks[]`. Подробности — `CLAUDE.md` проекта.
- Сценарий уже был пересобран 12.08.2026 по предыдущей аудиозаписи под тайминг
  30 минут (было 40). В `guide` уже есть раздел «Почему выходит 40 минут и как
  удержать 30» с разбором конкретных мест перегруза.
- Разбор новой записи (18.08.2026) показал: **те же самые перегрузы повторились
  почти один в один**, несмотря на то что в сценарии уже есть заметки против
  них — заметки лежат в скрытых `note` и на живом разговоре не помогают.

Конкретные повторившиеся проблемы (детали в чате, не в этом файле):

1. Калькулятор — снова подробный разбор обеих вкладок вместо 40 секунд.
2. Карты — снова перечислены все 9 по одной вместо «2 основные + 2 ловушки».
3. Боксы EVO/Telepass — снова разобран принцип работы по странам вместо одной фразы.
4. EUROVIGNETTE — снова названы «9 шагов формы».
5. «Поставьте базу на личный телефон» — снова повторено 4 раза подряд.

Новые проблемы, которых не было в предыдущем разборе:

6. Вступительная «Рамка» (блок 0) пропущена целиком — без вступительной фразы,
   без тайминга «полчаса / 6 тем».
7. Экран «Запреты движения» подробно разобран внутри блока 1 (телефоны), а
   обязательная строка-предохранитель «чего там нет» (экозоны, тоннели,
   перевалы, весенние ограничения по осям) не прозвучала.
8. Блок 5 «Красная линия» — видео не показано, рассказано словами; закрывающей
   фразы «это не про недоверие, это про то, что видно» нет.
9. Блок «Что сделать после» пропущен целиком: памятка, следующий инструктаж
   (отдел контроля), напоминание про «Написать в офис».
10. Согласование парковки/тоннеля «заранее и письменно» с именем менеджера —
    не прозвучало.

## Что предстоит решить в брифинге

Владелец просит не просто починить текст, а пересмотреть структуру: что-то
объединить, что-то убрать — раз одних заметок в `note` недостаточно, чтобы
живой инструктаж держал дисциплину сценария.
